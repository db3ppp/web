그 동안의 정적인 html에서는 페이지를 추가하려면 코드를 수정해야 했지만
nodejs를 이용하면 동적으로 코드수정 없이 추가할 수 있다. ex)단지 파일 추가만으로도.

콘솔에서 입력값 받기
var args = process.argv;
console.log(args);

querystring의 정보에 따라 웹페이지 동적으로 바꿔보기
1. 제목바꾸기
2. 본문내용 바꾸기 -> 파일읽어들여오는 방법으로
3. 목록부분 추가하기 ->디렉토리의 파일 목록 나타내는 방법으로 나타내고 링크걸기
